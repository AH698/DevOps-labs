name: linting a docker image 
on: [push]

jobs: 
    build: 
        runs-on: ubuntu-latest
        steps: 
            - name: checkout code 
              uses: actions/checkout@v4

            - name: lint Dockerfile
              uses: hadolint/hadolint-action@v3.1.0
              with: 
                dockerfile: task-1/Dockerfile